const menuButton=document.querySelector('.menu-button');const mobileMenu=document.querySelector('#mobile-menu');menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));menuButton.setAttribute('aria-label',open?'Open menu':'Close menu');mobileMenu.hidden=open});mobileMenu.addEventListener('click',event=>{if(event.target.matches('a')){mobileMenu.hidden=true;menuButton.setAttribute('aria-expanded','false')}});const toggle=document.querySelector('.enquiry-toggle');const form=document.querySelector('.enquiry-form');toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));form.hidden=open;if(!open)form.querySelector('input').focus()});form.addEventListener('submit',event=>{event.preventDefault();form.querySelector('.form-message').textContent='Thank you, Shyam will contact you shortly.';form.reset()});
